<h1>Make a new booking</h1>

<%= form_for [@booking.restaurant, @booking] do |f| %>
  <%= f.date_field :booking_date %>
  <%= f.time_field :booking_time %>
  <%= f.submit "Book a Table"%>

  <% end %>